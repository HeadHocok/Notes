


Notes.Application - вс€ логика запросов/получени€/создани€/редактировани€ (CRUD) заметок и валидаци€ этих запросов.

Notes.Application/Common - содержит инфраструктуру. ¬се что относитс€ к поведени€м, исключени€м, маппингу и т.д.

Notes.Application/Notes - содержит все команды и запросы св€занные с сущностью Note.

Notes.Domain - описание сущностей, бизнес-логика сущностей.


CQRS (Command Query Responsibility Segregation) - это архитектурный паттерн, который раздел€ет операции чтени€ (query) и операции записи (command) в системе. 